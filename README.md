# 🚦 Система детектирования дорожных знаков и классификации их состояний

Веб-приложение для автоматического детектирования дорожных знаков и классификации их состояний на изображениях с использованием современных методов компьютерного зрения и глубокого обучения.
---

## 🎯 Основные возможности

- **Загрузка изображений** через удобный веб-интерфейс
- **Автоматическое детектирование** дорожных знаков с помощью YOLOv8(21 класс типов знаков)
- **Классификация состояния знаков(нормальное/поврежденное/загрязнённое)** с использованием ResNet50
- **Визуализация результатов** с цветными bounding boxes в зависимости от состояния
- **Детальная статистика** по времени обработки и точности распознавания
- **Система логирования** всех операций распознавания

---

##  Технологический стек

### Backend
- **FastAPI** — асинхронный веб-фреймворк с автоматической документацией OpenAPI
- **Ultralytics YOLOv8** — детекция объектов (YOLOv8n)
- **PyTorch** — фреймворк глубокого обучения
- **ResNet50** - 
- **OpenCV** — обработка и визуализация изображений

### Frontend
- **Jinja2** — шаблонизация HTML
- **Чистый CSS/JavaScript** — адаптивный интерфейс
- **Bootstrap** элементы для стилизации

---

## Структура проекта

```
Project/
├── app/                           # Основное веб-приложение 
│   ├── templates/                 # HTML-шаблоны 
│   │   ├── index.html             # Главная страница 
│   │   ├── detect.html            # Страница детектирования
│   │   └── logs.html              # Страница истории всех выполненных анализов
│   ├── static/                    # Статические файлы (загруженные и обработанные изображения)
│   │   ├── uploads/               # Временное хранилище загруженных пользователем изображений
│   │   └── results/               # Генерируемые изображения с визуализацией результатов
│   ├── logs/                      # Директория для хранения файлов журнала 
│   ├── main.py                    # Запуск FastAPI-сервера 
│   ├── models.py                  * Ядро системы: класс SignDetector(YOLOv8+ResNet50)*
│   └── routes.py                  * Логика маршрутов *
│
├── data_preparation_model_training/ # Скрипты и файлы, необходимые для подготовки данных и обучения моделей
│   ├── runs/                      # РезультатЫ обучения YOLO (логи, веса)
│   ├── preparingDataset.py        # Скрипт для предобработки и аугментации данных
│   ├── model.py                   * Скрипт для обучения модели детектирования YOLOv8 на датасете data*
│   ├── model2.py                  * Скрипт для обучения классификатора состояний ResNet50 на датасете state_dataset*
│   └── data.yaml                  * Конфигурационный файл YOLO *
│
├── models/                        # Директория для хранения обученных весов моделей
│   ├── best.pt                    * Финальные веса модели YOLOv8 для детектирования знаков*
│   └── best_state_classifier.pth  * Финальные веса модели ResNet50 для классификации состояния*
│
├── data/                          # Размеченный датасет в формате YOLO для задачи детектирования
│   ├── train/                     # Обучающая выборка
│   │   ├── images/                # Изображения для обучения
│   │   └── labels/                # Аннотационный файлы с разметкой (bounding boxes)
│   ├── valid/                     # Валидационная выборка
│   │   ├── images/
│   │   └── labels/
│   └── test/                      # Тестовая выборка
│       ├── images/
│       └── labels/
│
└── state_dataset/                 # Датесет для классификации состояния
    ├── train/                     # Обучающая выборка
    │   ├── damaged/               # Изображения поврежденных знаков
    │   ├── dirty/                 # Изображения загрязненных знаков
    │   └── normal/                # Изображения знаков в нормальном состоянии
    ├── valid/                     # Валидационная выборка 
    │   ├── damaged/
    │   ├── dirty/
    │   └── normal/
    └── test/                      # Тестовая выборка 
        ├── damaged/
        ├── dirty/
        └── normal/
```

---


## Результаты модели

**Обученная модель детектирования показала следующие метрики на валидационной выборке:**

| Метрика      | Значение | Описание                     |
|--------------|----------|-----------------------------|
| **Precision** | 0.969    | Высокая точность детекции     |
| **Recall**   | 0.917    | Хорошая полнота обнаружения  |
| **mAP@0.5**  | 0.961    | Общая эффективность модели   |

---

---
#Также обученная модель классификации показала высокую точность на валидационном наборе - 99.94%, что говорит о хорошем качестве классификации.
---

## API Endpoints

### Основные endpoints:
- `GET /` — Главная страница с формой загрузки изображения
- `POST /detect` — Обработка загруженного изображения и распознавание дорожного знака
- `GET /logs` — Просмотр истории распознаваний

---

##  Особенности реализации

### 1. **Двухэтапный пайплайн обработки**
```python
# 1. Детекция с помощью YOLO
results = yolo_model(image, conf=0.3)

# 2. Классификация состояния с помощью ResNet50:
state = resnet50_model(plate_crop)
```

### 2. **Постобработка результатов**
- Транслитерация латинских символов в кириллические
- Валидация по шаблонам дорожных знаков
- Фильтрация шумовых символов

### 3. **Адаптивная архитектура**
```python
# Автоматический выбор устройства для вычислений
device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
# Модели автоматически используют GPU при наличии
```#   R o a d S i g n C o n d i t i o n 
 
 #   C o u r s e w o r k 
 
 #   R o a d S i g n C o n d i t i o n  
 